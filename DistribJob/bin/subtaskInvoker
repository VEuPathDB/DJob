#!/bin/sh

localdir=$1
serverdir=$2

shift 2

cd $localdir

$* > subtask.output 2> subtask.stderr    ## stdout goes into subtask.output and stderr into subtask.stderr

status=$?


if [ $status != 0 ]; then
  touch $serverdir/failed
  cp -r $localdir/* $serverdir
fi

touch $serverdir/done

